using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class NewRoundGuiChanger : MonoBehaviour
{
    //Get the round counter text item.
    [SerializeField]
    [Tooltip("Drag and drop the the text for the round GUI.")]
    TMP_Text RoundLevelCounter;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
